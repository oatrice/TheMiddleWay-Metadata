# คู่มือทดสอบ (Testing Guide)

> **Feature:** 🔐 [Feature] ระบบสมาชิกและการรักษาความปลอดภัย (User Authentication & Sync)
> **Issue:** #14

## สิ่งที่ต้องทดสอบ (To Test)

### 1. การสมัครสมาชิก / เข้าสู่ระบบด้วย Google
- **เป้าหมาย:** ผู้ใช้สามารถผูกบัญชี Google ลงฐานข้อมูล PostgreSQL
- **วิธีทดสอบ:**
  1. เปิดแอป/เว็บ
  2. กดปุ่ม Login / Profile
  3. กดปุ่ม `Continue with Google`
  4. เลือก Account Google
- **ผลลัพธ์ที่คาดหวัง:**
  - เข้าสู่ระบบสำเร็จ 
  - ชื่อ และ Profile รูปภาพโหลดขึ้นมาแสดงอย่างถูกต้องในหน้า Profile

### 2. การตรวจสอบ Backend Network (สำคัญ)
- **เป้าหมาย:** Data Profile ต้องถูกบันทึกลง Postgres ผ่าน REST API เท่านั้น
- **วิธีทดสอบ (Web):**
  1. เปิด Browser DevTools (Network Tab)
  2. กรอง Network request ด้วยคำว่า `/api/v1/auth/sync`
  3. ทำการ Login ด้วย Google
- **ผลลัพธ์ที่คาดหวัง:**
  - จะต้องเห็น Request `POST /api/v1/auth/sync` ไปที่ Server (หรือ localhost `8080`)
  - Status Code ของ Request นี้ต้องเป็น `200 OK`
  - ❌ **ห้าม**พบ Request มุ่งไปสู่ `firestore.googleapis.com` เด็ดขาด

### 3. ตรวจสอบการอัปเดตข้อมูลบน Database ให้ตรง (Source of Truth)
- **เป้าหมาย:** สังเกตการอัปเดตเวลาลดความซ้ำซ้อนในตาราง
- **วิธีทดสอบ:**
  1. ปิดแอปฯ (หรือ Refresh Web)
  2. ลอง Login ซ้ำด้วยบัญชี Google บัญชีเดิม
- **ผลลัพธ์ที่คาดหวัง:**
  - ข้อมูลในฐานข้อมูล (Postgres database ตาราง `users`) ช่อง `last_login_at` ต้องอัปเดตเป็นเวลาล่าสุด
  - จำนวน ID และจำนวน Row ในตารางต้องไม่ซ้ำกัน (ใช้ ID เดิม)
